# Install the base requirements first, then replace CPU ONNX Runtime:
#   pip install -r requirements.txt
#   pip uninstall -y onnxruntime
#   pip install -r requirements-gpu.txt
onnxruntime-gpu==1.24.4
